
const createChassis = () => {
    //Create new object. No properties yet.
    const newChassisObject = {};

    //if your function passes a result you must store it in a variable.
    return newChassisObject;
};

module.exports = {createChassis};